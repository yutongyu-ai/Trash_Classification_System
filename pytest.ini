[pytest]
testpaths = tests
markers =
    integration: hits real external services (Hugging Face Hub model download, torchvision pretrained-weight download). Not required for CI to pass; run in a separate non-blocking job.
